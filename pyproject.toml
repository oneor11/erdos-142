[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "erdos142"
version = "0.1.0"
description = "Exact arithmetic-progression-free subset experiments"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
